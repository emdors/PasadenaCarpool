doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    title Pasadena Carpool Czar Tools
    script(src='//ajax.googleapis.com/ajax/libs/angularjs/1.3.3/angular.min.js')
    script(src='//cdnjs.cloudflare.com/ajax/libs/angular.js/1.3.3/angular-route.min.js')
    script(src='//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js')
    link(rel='stylesheet', href='http://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css')
    script(src='//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/js/bootstrap.min.js')
    link(rel='stylesheet', href='css/czarstyle.css')
  body
    div
      div
        nav.navbar.navbar-inverse(role='navigation', style='padding-left:130px;')
          ul.nav.navbar-nav
            li
              a(href='/') Home
            li
              a(href='/schedule') Schedule
            li.active
              a(href='/czar') Czar
      br
      .container.theme-showcase(role='main')
        h1 Form Results
        if formResults.length == 0
          | None yet
        else
          //-each dayData, day in peoplesTimes
          each dayData in peoplesTimes
            h3= dayData.day
            //-each AMPMData, AMPM in dayData
            each AMPMData in dayData.times
              - AMPM = AMPMData.halfday
              h4= AMPM
              table.timetable
                tr.timetext
                  td
                  each hr in possibleDriveHours[AMPM]
                    td.hour(colspan='2')= hr
                    td.halfhour(colspan='2') :30
                  td
                //-each personData, name in AMPMData
                each personData in AMPMData.people
                  tr(class='timeview')
                    td.name= personData.name
                    //-each canGo, min in personData.canGos
                    each hr in possibleDriveHours[AMPM]
                      each min in [0, 15, 30, 45]
                        td(class=personData.canGos[hr*100+min])
                        //-td= JSON.stringify(personData)
                    //-each hr in possibleDriveHours[AMPM]
                    //-  each min in [0, 15, 30, 45]
                    //-    //-td(class=personData.canGos[(hr*100+min).toString()])
                    //-    td= personData.canGos[400]
                    unless personData.driveStatus == 'can'
                      td.driveStatus= personData.driveStatus


          //-ul
          //-  each day in weekdays
          //-    h3= day
          //-    each hours, AMPM in possibleDriveHours
          //-      h4= AMPM
          //-      table.timetable
          //-        tr.timetext
          //-          td
          //-          each hr in hours
          //-            td.hour(colspan='2')= hr
          //-            td.halfhour(colspan='2') :30
          //-          td
          //-        - todayTimeName = day + AMPM + 'Times'
          //-        each thisPersonsTimes in peoplesTimes
          //-          tr(class='timeview')
          //-            //td.name= formSub.name
          //-            td.name= thisPersonsTimes.
          //-            each hr in hours
          //-              each min in [0, 15, 30, 45]
          //-                td(class=thisPersonsTimes.times[hr][min])
          //-            td.driveStatus= thisPersonsTimes.driveStatus

          h2 Other Information
          ul
            each result in formResults
              li
                table(style="table-layout: fixed; width: 100%")
                  each value,key in result
                    if key.substr(-'Times'.length) != 'Times' && key.substr(-'DriveStatus'.length) != 'DriveStatus'
                      tr
                        td= key
                        td= value
